# Move Basic

Minimal Aptos Move package used to demonstrate local compile validation.

- `Move.toml` defines a small package.
- `sources/hello.move` exposes one public entry function.
- Run `devflow validate examples/move-basic/Move.toml` after installing Aptos CLI.
