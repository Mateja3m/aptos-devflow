# Valid Online Simulate

Uses the online validation mode and the Aptos RPC simulation rule.

If `APTOS_RPC_URL` is not configured, online checks are reported as skipped warnings and the relaxed profile still treats the fixture as a pass.
